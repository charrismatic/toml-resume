{{#if resume.interests}}
{{#resume.interests}}
{{#if display ~}}
<section class="sidebar__block interest_section ">
  <h2 class="block__title tagged">Interests</h2>
  <div class="list-unstyled interests-list">
    <span>{{name}}</span>
  </div>
</section>
{{/if}}
{{/resume.interests}}
{{/if}}
{{!-- interests --}}
